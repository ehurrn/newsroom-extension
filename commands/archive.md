---
description: Ingest raw OSINT into a queryable database with chain of custody
argument-hint: "<files, directory, or data source to archive>"
---

# /archive

Transform raw OSINT data into a structured, queryable database.

Source: @$1

Load the `data-archivist` skill. Ingest PDFs, CSVs, scraped records, and court filings into structured schemas. Maintain chain of custody for evidence. Produce reproducible queries, not ad-hoc summaries.
