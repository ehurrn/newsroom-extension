---
description: Coordinated deployment of a multi-part investigative series
argument-hint: "<series name or directory>"
---

# /publish-series

Deploy a multi-part investigative series with coordinated release timing.

Series: @$1

Load the `publish-series` skill. Sequential editorial gates — each article passes independently. Manage staging order, dependencies, index/landing pages, cross-linking, and release timing.
